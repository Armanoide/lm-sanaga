use sn_core::dto::model_runtime::ModelRuntimeDTO;
use sn_inference::model::model_runtime::ModelRuntime;

pub trait ModelRuntimeExt {
    // fn from(model_runtime: &ModelRuntime) -> ModelRuntimeDTO;
}
