use crate::error::Error;
use crate::model::model::Model;
use crate::model::weight::Weight;

/*
pub struct ModelMistral {
    config: Config,
    weight: Weight,
}

impl Model for ModelMistral {

    fn sanitize(&mut self) {
        todo!()
    }

    fn quantized(&self) {
        todo!()
    }

    fn supports_quantization(&self) -> bool {
        self.config.quantization.is_some()
    }
}

impl ModelMistral {
    pub fn new(config: Config, weight: Weight) -> Result<ModelMistral, Error> {
        Ok(ModelMistral {
            config,
            weight,
        })
    }
}*/
